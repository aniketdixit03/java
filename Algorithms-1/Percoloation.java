public class Percoloation {
    /*The problem consists of an N*N grid, where the grid is said to be percolated if there is a way using
    which one can reach the top row from the bottom row. The catch is that each box in the grid is having a p
    probability denoting its openess to being used or 1-p to its resistance for the same.
    */
    /*In this problem and in many similar problems, there's what's called a phase transition. Which says that, you know, 
    when it's low, it's not going to percolate. When it's high, it is going to percolate. And actually, the threshold 
    between when it percolates and when it doesn't percolate is very sharp. And actually there is a value as N gets large 
    that if you're less than that value it almost certainly will not percolate, if you're greater it almost certainly will. 
    The question is what is that value. This is an example of a mathematical model where the problem is, is very well 
    articulated. What's that threshold value but, nobody knows the solution to that mathematical problem. The only solution 
    we have comes from a computational model, where we run simulations to try and determine the value of that probability. 
    And those simulations are only enable by fast union find algorithms.*/
    //Solution to above discussion comes in the form of Monte Ca
}

//How to solve the above Percolation problem for N*N grid in Java using Quick Find Algorithms?
