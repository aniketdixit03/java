while (!stacks.isEmpty()) {
        //     char s = sc.next().charAt(0); //pay attention to this line
        //     if("o".equals(s)) stacks.pop(); 
        //     else stacks.push(s);
        //     sc.close();
        // }